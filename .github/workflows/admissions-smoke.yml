name: Admissions Reports Smoke

on:
  pull_request:
    paths:
      - 'admissions/**'
      - '.github/workflows/admissions-smoke.yml'
  push:
    branches:
      - main
    paths:
      - 'admissions/**'
      - '.github/workflows/admissions-smoke.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate static admissions publication
        run: node admissions/smoke.mjs
