name: 3lines-d3-temp-verify
on:
  pull_request:
    branches: [main]
    paths:
      - 'tools/3lines/**'
      - '.github/workflows/3lines-d3-temp-verify.yml'

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    defaults:
      run:
        working-directory: tools/3lines
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm ci --ignore-scripts
      - name: Unit tests
        run: npm test
      - name: Quality invariants
        run: npm run quality
      - name: JavaScript syntax
        run: |
          node --check src/composer.js
          node --check src/fallback.js
          node --check src/main.js
          node --check src/summarizer.js
          node --check vendor/tiny-summarizer-tf.js
      - name: Deterministic composer semantic probe
        run: node tests/composer-probe.mjs
      - name: Dependency audit
        run: npm audit --audit-level=high
      - name: Install Chromium for one mobile smoke
        run: npx playwright install --with-deps chromium
      - name: Mobile smoke
        run: npm run smoke
